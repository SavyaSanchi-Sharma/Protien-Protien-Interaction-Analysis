# ──────── Python ────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.Python

# ──────── Virtual environments ────────
venv/
env/
.venv/
.env/
ENV/

# ──────── IDE / editor / tooling ────────
.vscode/
.idea/
.codex
.codex/
.claude/
*.swp
*.swo
.DS_Store

# ──────── Large data (regenerable; keep code & manifests only) ────────
# PDB structures and PLM embeddings — regenerate via dataprep.sh
data/pdbs/
data/esm/
data/protbert/
data/esm_multi/
data/protbert_multi/

# Derived training-time artefacts in data/structural/
data/structural/Train_split_train.csv
data/structural/Train_split_val.csv
data/structural/*.partial
data/struct_norm.npz
data/*.npz

# Other downloaded / extracted dataset directories
PDBtest164/
dset186/
dset72/
results/

# ──────── Per-model generated artefacts ────────
# Match anywhere in the tree (covers active models + vintage/).
**/checkpoints/
**/analysis/

# Per-run training/eval outputs (regenerable from best.pt + test.py)
**/training_log.json
**/test_results.json

# Tune outputs — best_hp.json IS tracked by default for reproducibility.
# Uncomment the next line if you'd rather not version-control them.
# **/best_hp.json

# ──────── Runtime logs ────────
logs/
*.log

# ──────── Packages / installers ────────
*.deb
*.rpm
*.tar.gz
*.zip

# ──────── Reference / vendored binaries ────────
cuda-keyring_*.deb
esm_model_weights/
protbert_model_weights/

# ──────── Misc large reference files ────────
*.pdf

# ──────── Local snapshots (separate dir) ────────
# Snapshots live at ../Model_snapshots — outside this repo, ignored implicitly.
